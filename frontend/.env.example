# Copy to .env and fill in real values. Never commit .env itself.

VITE_API_URL=http://localhost:8000/api

# Publishable (public) key - safe to expose client-side. Reserved for
# a future Stripe.js/Elements integration; not read by any code yet.
VITE_STRIPE_PUBLISHABLE_KEY=
